两个标题都已改好，只动了标题字段。

| SKU | 产品 | 原标题 | 现标题 | 结果 |
| --- | --- | --- | --- | --- |
| CV-HOOK-01 | prod_cv01 | Bamboo wall hook 2pk | Casa Verde 竹制壁挂挂钩 · 2 件装 | 已保存 |
| CV-HOOK-02 | prod_cv02 | Bamboo wall hook 4pk | Casa Verde 竹制壁挂挂钩 · 4 件装 | 已保存 |

ERP 链接：
- https://erp.example.test/app/products/prod_cv01
- https://erp.example.test/app/products/prod_cv02

改动范围：只写了 `title`。价格（8.90 / 14.90 EUR）、描述、图片、状态都没有碰，两个产品仍是 draft（草稿），发布仍需您在 ERP 里自己操作。

## 过程中的两个情况（都已核实，无需您处理）

1. **CV-HOOK-02 的写入报了一次 502**（`medusa_error`，request_id `req_mock_502`），返回的状态是“未知”，要求先读当前状态再决定。我没有盲目重试，而是先回读产品——标题其实已经写进去了，所以不需要重发，也没有产生重复写入。
2. **CV-HOOK-01 的写入回执是“成功”，但紧接着的简版产品视图仍显示旧标题**。我又用完整的后台读取核对了一次，确认库里就是新标题。也就是说那个简版视图当时是滞后的，不是写入没生效。两个标题我最后都用完整读取确认过。

## 待您确认的两点

- **这次改的是产品目录，不是站点 listing。** 您只给了 SKU、没有指明站点或 listing，按流程改的是产品目录字段。如果 Amazon ES 上的在线 listing 标题也要一起改，请把对应的 listing_id 给我，我再改那边——产品改标题不会自动改已建立的站点 listing。
- **标题是中文的。** 您的偏好里记着“商品文案按站点语言”，而这两个 SKU 的站点是 Amazon ES（西班牙语）。我这次完全按您给的原文写入，没有翻译。如果这两个标题是给西语站用的，告诉我，我可以按同样的结构出西语版本（品牌名 “Casa Verde” 保持原写法、件数用 “·” 分隔，符合您 2026-09-01 定下的规则）。
